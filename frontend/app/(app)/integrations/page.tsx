"use client";

import { useCallback, useEffect, useState } from "react";
import { Check, MessageCircle, Phone, PlugZap, ShieldCheck, Unplug } from "lucide-react";
import { ErrorState, LoadingState, PageHeader } from "@/components/page";
import { Badge, Button, Card, CardDescription, CardHeader, CardTitle, Input, Spinner } from "@/components/ui";
import { api } from "@/lib/api";
import type { WhatsAppIdentity } from "@/lib/types";

export default function IntegrationsPage() {
  const [identity, setIdentity] = useState<WhatsAppIdentity | null>(null);
  const [phone, setPhone] = useState("");
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const load = useCallback(async () => { setLoading(true); setError(null); try { setIdentity(await api.whatsappIdentity()); } catch (reason) { setError(reason instanceof Error ? reason.message : "Não foi possível carregar as integrações."); } finally { setLoading(false); } }, []);
  useEffect(() => { void load(); }, [load]);
  const link = async () => { setSaving(true); setError(null); try { if (!phone.trim()) throw new Error("Informe o número com DDD."); setIdentity(await api.linkWhatsApp(phone)); setPhone(""); } catch (reason) { setError(reason instanceof Error ? reason.message : "Não foi possível vincular o WhatsApp."); } finally { setSaving(false); } };
  const unlink = async () => { if (!window.confirm("Desvincular este número do seu espaço?")) return; setSaving(true); try { await api.unlinkWhatsApp(); setIdentity({ linked: false, phone_e164: null }); } catch (reason) { setError(reason instanceof Error ? reason.message : "Não foi possível desvincular."); } finally { setSaving(false); } };
  return <>
    <PageHeader eyebrow="Conecte o que já faz parte da sua rotina" title="Integrações" description="Registre movimentos e consulte seu mês por conversa, mantendo o backend como fonte de verdade." />
    {loading ? <LoadingState label="Verificando conexões" /> : error && !identity ? <ErrorState message={error} onRetry={() => void load()} /> : <div className="grid gap-5 xl:grid-cols-[1.1fr_0.9fr]"><Card className="overflow-hidden"><CardHeader><div className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-[#dcf8e8] text-[#287853]"><MessageCircle size={22} /></span><div><CardTitle>WhatsApp</CardTitle><CardDescription>Baileys no desenvolvimento · Cloud API no futuro</CardDescription></div></div><Badge tone={identity?.linked ? "whatsapp" : "neutral"}>{identity?.linked ? "Conectado" : "Não conectado"}</Badge></CardHeader><div className="px-5 pb-6 md:px-6">{identity?.linked ? <div className="rounded-2xl border border-[#b8e8ca] bg-[#f3fcf6] p-5"><div className="flex items-start gap-3"><div className="mt-0.5 flex h-8 w-8 items-center justify-center rounded-full bg-[#dcf8e8] text-[#287853]"><Check size={16} /></div><div className="flex-1"><p className="text-sm font-semibold text-ink">Número vinculado</p><p className="mt-1 text-sm text-muted">{identity.phone_e164}</p><p className="mt-3 text-xs leading-5 text-muted">Agora o agente consegue identificar suas mensagens e encaminhar operações para suas contas.</p></div></div><Button variant="secondary" size="small" className="mt-5" onClick={() => void unlink()} disabled={saving}><Unplug size={14} /> Desvincular</Button></div> : <><p className="text-sm leading-6 text-muted">Vincule seu número para que mensagens como <span className="font-semibold text-ink">“Gastei 42 reais no almoço.”</span> cheguem ao seu espaço financeiro certo.</p><div className="mt-5 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Phone size={16} className="pointer-events-none absolute left-3 top-3.5 text-muted" /><Input value={phone} onChange={(event) => setPhone(event.target.value)} placeholder="+55 92 99999-9999" className="pl-9" /></div><Button onClick={() => void link()} disabled={saving}>{saving ? <Spinner /> : <PlugZap size={16} />} Vincular número</Button></div><p className="mt-3 text-xs text-muted">Use o formato internacional. A vinculação exige login e não expõe seu user_id ao agente.</p></>}{error && <p role="alert" className="mt-4 text-sm text-rust">{error}</p>}</div></Card><Card className="overflow-hidden"><CardHeader><div><p className="eyebrow">Como funciona</p><CardTitle className="mt-1">Conversas com segurança</CardTitle></div><ShieldCheck size={20} className="text-moss" /></CardHeader><div className="space-y-5 px-5 pb-6 md:px-6"><Step number="01" title="Você envia" text="Uma mensagem natural chega pelo WhatsApp Web." /><Step number="02" title="O agente interpreta" text="O Hermes escolhe uma tool; o modelo não acessa seu banco." /><Step number="03" title="A API valida" text="O FastAPI resolve sua identidade, valida valores e grava no PostgreSQL." /><Step number="04" title="Você acompanha" text="O resultado aparece no dashboard e pode ser consultado de novo." /></div></Card></div>}
  </>;
}

function Step({ number, title, text }: { number: string; title: string; text: string }) { return <div className="flex gap-3"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-lg bg-paper text-[0.65rem] font-bold text-navy">{number}</span><div><p className="text-sm font-semibold text-ink">{title}</p><p className="mt-1 text-sm leading-5 text-muted">{text}</p></div></div>; }
